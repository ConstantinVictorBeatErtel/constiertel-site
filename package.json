{
  "name": "constiertel-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "cf:preview": "npm run build && wrangler pages dev dist",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.1.7"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
